{
  "name": "costvista",
  "version": "1.0.0",
  "main": "index.js",
    "scripts": {
    "dev": "concurrently -k -n FRONT,BACK -c auto \"npm:dev:frontend\" \"npm:dev:backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "bash -lc 'cd backend && source .venv/bin/activate && python -m uvicorn main:app --reload --port 8000'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
